spring:
  application:
    name: transaction-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      retry:
        max-attempts: 6
        max-interval: 2000

logging:
  pattern:
    correlation: "[%X{correlationId:-}]"